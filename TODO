- bien close les fds + >> a faire + creer si plusieurs outfile
- here_doc a modif
- ./ a faire

< Makefile ls -la | wc -l > outfile
< Makefile ls -la | wc -l | wc -l > outfile
< Makefile ls > outfile

cat << EOF
echo test << EOF
...