- lstat = check si fichier est un directory avant d effectuer la redirection
- chmod 0 sur un fichier test : doit afficher code erreur 
- <Makefile | cat
- voir si peux pas fix error ctrl+c
- utiliser perror pour afficher les erreurs : exemple infile inexistant : perror(file_name)
- signal(SIGQUIT, SIG_IGN); pour ignorer ctrl + \ et idem CTRL+C apres les remettre
- exit l 42 = faut quitter, exit 42 42 = faut pas quitter
- ./fsdkjfh mauvais message d erreur
- chemin absolument doit executer anyways
- cat et CTRL + C : doit afficher ctrl c + affiche pas instant 131

- echo > | cat = parsing
- export a => afficher dans export mais pas env, mais export a=(chaine vide) a afficher dans les 2
- si on fait un export a alors que a a une value, on remplace pas la value
- segfault sur l'unset du premier et seul element de la liste de variable d environnement puis action